apiVersion: v1
kind: Service
metadata:
  name: vpromq01
  labels:
    app: vpro-mq
spec:
  selector:
    app: vpro-mq
  ports:
    - port: 5672
      targetPort: vpromq-port
      protocol: TCP
  type: ClusterIP
