apiVersion: v1 
kind: Service
metadata:
  name: vprocache01
  labels:
    app: vpro-mc
spec:
  selector:
    app: vpro-mc
  ports:
    - port: 11211
      targetPort: vpromc-port
      protocol: TCP
  type: ClusterIP
