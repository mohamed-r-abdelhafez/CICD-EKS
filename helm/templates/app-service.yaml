apiVersion: v1
kind: Service
metadata:
  name: vproapp-service
spec:
  selector:
    app: vpro-app
  ports:
    - port: {{ .Values.app.servicePort }}
      targetPort: {{ .Values.app.servicePort }}
      protocol: TCP
  type: LoadBalancer
